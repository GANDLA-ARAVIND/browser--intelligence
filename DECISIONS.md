# Decision log

The measurement record for [CLAUDE.md](CLAUDE.md). Every row here explains
**why** something is the way it is — what was measured, what was tried and
rejected, what a number actually came out at.

Rules that constrain future work do **not** live here; they are in CLAUDE.md
§14. The test for which file a row belongs in: does it tell a future
implementer what they *must do*, or does it explain *why* something is the way
it is? Must-do stays in CLAUDE.md, explains-why lives here.

**The §0 propagation rule spans both files.** When a row here corrects a
CLAUDE.md section, update that section in the same edit. A decision recorded
here while the spec section still says the old thing is exactly the drift this
split was made to prevent — the record grows monotonically, so an uncorrected
section stays wrong forever while looking authoritative.

Append new rows at the end. **Never rewrite a row to match a later finding that
changes its conclusion** — add a superseding row and say what it replaces, so
the reasoning stays auditable. Extending a row with further evidence for the
*same* conclusion is fine and preferred; that is accumulation, not revision.

| Decision | Rationale |
|---|---|
| No backend | Privacy is the differentiator; also removes cost and auth entirely |
| No vector DB | 15MB of vectors, measured 7.3ms brute-force scan at 10k — a DB is pure overhead |
| Derived taxonomy | A hand-written topic list only ever fits its author |
| No productivity score | Invented metrics are detectable and destroy trust in honest features |
| Extension page, not hosted dashboard | Same React work, zero server, privacy intact |
| Chromium-only v1 | ~75% of desktop users from one build; Firefox is 2–3% |
| Day boundary at 04:00 | Late-night work belongs to the previous day |
| Phase 0 clusters with mutual-kNN + shared-neighbour, not HDBSCAN | HDBSCAN needs a UMAP step to behave on 384 dims and has no trustworthy JS port; `--algo community` and `--algo kmeans` remain as comparison arms |
| Shared-neighbour test on every graph edge | Mutual-kNN alone chains: one accidental link welds two topics into one blob under connected components. Requiring shared context breaks bridges, which by definition have no mutual crowd |
| `scripts/export-history.mjs` reads Chrome's local SQLite | Takeout takes hours and drops `visitCount`; `node:sqlite` is built in, so this stays zero-dependency and fully local |
| Cluster output displays the original title, embeds the stripped one | react.dev titles every page "… – React", so the suffix stripper removes the one word that identifies the cluster. Measured: stripping costs 0.33 of within-group similarity and 63% of the topic/unrelated gap. Clustering still works; a human reading the output could not tell what the cluster was, which defeats the §11 exit criterion |
| **Encryption at rest downgraded, consciously — the threat model does not support it** | §9 originally required it; it was never built, and on inspection it should not be. The index sits in extension-local IndexedDB inside the Chrome profile directory, under the same OS permissions as Chrome's own `History` SQLite, saved passwords and session cookies. **An attacker who can read our store can already read all of those, every one of them higher-value.** Encrypting ours while they sit in plaintext beside it is security theatre. Worse, there is no key we can hold that such an attacker cannot: with no user passphrase in the product the key lives in `chrome.storage` — same profile, same permissions — so it is obfuscation, and shipping obfuscation *labelled* "encrypted" is worse than shipping nothing, because it invites trust it cannot earn. The controls that genuinely defend the user are already in §9 and are all pre-storage or user-driven: blocklist before extraction, pause, retroactive delete, separate Chrome profile. **v2 candidate:** a user-supplied passphrase is the one key an attacker with profile access lacks — but it changes the product, not the storage layer. A locked index cannot be searched, so it needs an unlock flow, a policy for capture while locked, and a forgotten-passphrase answer better than "your history is gone" |
| The quality verdict **controls** the extraction ladder; it does not annotate it | Recording quality and embedding the text anyway was the bug: Readability returned 519 chars of nav chrome and month names for a GitHub profile, scored it `poor`, and stored it regardless. A rung is now accepted the moment it is not `poor`, and the recorded tier is the one that actually **supplied** the text, not the last attempted. When every rung is poor the highest-scoring one wins rather than the last — the GitHub metadata description (0.54) beats its nav bar (0.14). Rungs are thunks so a rung never runs unless the ladder reaches it, which matters once step 2 adds tier-1 adapters that fetch transcripts |
| SPA navigation is detected by polling `location.href`, not by patching history | Most modern sites (YouTube, LinkedIn, Reddit, GitHub, Naukri) navigate via `pushState` with no document load, so load-time capture silently misses every page after the first — potentially a large fraction of browsing. Three options weighed: `chrome.webNavigation.onHistoryStateUpdated` is authoritative but costs a permission and a round trip; **monkey-patching `history.pushState` from a content script does not work at all**, because content scripts run in an isolated world with their own `history` binding and never see the page's calls — making it work needs a MAIN-world injection that many sites' CSP blocks. Polling won: the 1s dwell tick already exists, so it costs one string comparison, no permission, and no new failure mode, and it observes the *result* so it catches every navigation mechanism. Dwell resets on change, so 30s on video A cannot qualify video B. Fragment-only changes are ignored because `normalizeUrl` strips them anyway; `#/` routes are kept |
| **`coverage` measures completeness, not correctness** | A YouTube watch page scores good 0.91 at **100% coverage** — and the text is the video description plus the sidebar recommendation list. 100% means Readability extracted everything the page offered; it says nothing about whether the page offered the right thing. Coverage can only ever detect *under*-extraction. This is the failure class no structural threshold reaches, because the output is well-formed prose about the wrong subject, and it is the whole justification for the YouTube transcript adapter |
| A site named as needing an adapter on the evidence of one page was wrong | GitHub was listed for a tier-1 adapter after a *profile* page returned nav chrome and month names. Repo pages then measured good 0.87 / 7,412 chars / 82% coverage, and 7,998 chars of real README on another. Only profile and sub-tab pages fail, and the ladder already demotes those. The correction is cheap here; the general point is that one unrepresentative page is enough to justify building the wrong thing, and the adapter budget is 5–8 total (§8) |
| Text over ~115 units with zero sentence terminators is not prose | A structural fact about writing, not a tuned threshold: concatenated UI labels satisfy every other signal and produce not one terminator in 700 characters. Applied only where the script actually uses terminators, since Thai, Lao, Khmer and Myanmar do not. This is what finally catches the Naukri case independently of the stopword fix |
| Superseded — repeated UI labels defeat the stopword signal (fixed, see above) | A stored Naukri "Application History" capture (984 chars of `Recruiter Actions(0)Applies on Naukri(69)…Prep for this interviewSoftware EngineerPrep for this interview…`) re-scores **weak, 0.568** — not `poor`, so the ladder accepts it at tier 2 and embeds it. The stopword ratio came out **0.324, inside prose range**, because `"Prep for this interview"` repeated five times contributes *for* and *this* each time: repetition inflates the ratio without indicating prose. The signal that did fire was terminator density at **0.0**, but its 0.25 weight is not decisive. Candidate rule for later, deliberately not applied yet: 700+ characters with *zero* sentence terminators is structurally not prose regardless of other scores. Also open — whether `weak` should trigger fall-through when a lower rung scores better, not just `poor` |
| Extraction tier records method, not quality — they need separate fields | All six pages in the first capture test reported tier 2 (Readability), but a GitHub profile yielded 519 chars of nav chrome and month names, NeetCode yielded a 248-char announcement banner, and YouTube yielded the video description rather than the transcript. §8's coverage metric would have reported **100% success on a corpus where a third was unusable**. `extractionQuality` is now stored alongside the tier. Optional by design: backfilled pages had no extraction to assess |
| Structural quality signals catch non-prose, never wrong-prose | Measured on reconstructions of the six: **stopword ratio is the strongest discriminator** — nav chrome 0.08 against 0.38–0.63 for real prose — with absolute length catching the short banner. But the YouTube description scores 0.75 "good" and is still the wrong content, because it *is* prose. No structural metric will catch that; it needs a site adapter. Two traps found while building it: "mean words per sentence" runs **backwards** (text with no terminators becomes one enormous sentence, so a nav bar scored a perfect 1.0 — replaced with terminator density), and the English stopword list must be skipped entirely for non-Latin text rather than substituted with a neutral value, or every Telugu page scores as chrome |
| §5's "no vector DB" arithmetic holds, with margin | Brute-force top-20 cosine over normalized vectors, medians of three: **2,670 nodes 2.17ms · 5,747 nodes 4.51ms · 10,000 nodes 7.31ms · 50,000 nodes 33.7ms**. §5 predicted ~12ms at 10k, so the estimate was conservative by 1.6×. Linear in node count, as expected. At 50k it is still a third of a frame — a vector DB would remain pure overhead for years of browsing. Scanning collapsed representatives rather than all pages also stops twenty near-identical LeetCode rows filling the results |
| The width cap flipped the blocking ranking: collapse is now the dominant blocker | Post-fix medians in the extension: embed **96.4s** (was 133.8s), longest embed stall **1.3s** (was 17.1s), total **109.3s** (was 149.7s). Collapse now blocks **11.9s unbroken** against embed's 1.3s longest — the ranking inverted, exactly as the batch-size rule predicts: capping width shrank the per-batch unit of work while collapse remained a single synchronous pass over the whole corpus. Collapse is the Phase 3 responsiveness target |
| A stall offset is measured against the attributed *segment*, never a stage looked up by name | The dashboard reported "began −6ms into collapsing", which is impossible. A gap almost always straddles a boundary — the tick before a long block fires a few ms before the stage switches — and resolving the stage's start by *name* afterwards can return an earlier occurrence. `attribute()` now returns the winning segment's start alongside its label, and the offset is clamped at zero. Note the fix is in the offset, not the attribution: overlap already assigned the 11.9s block to collapse correctly, and reattributing it to the previous stage on the strength of a 6ms boundary artifact would have restored the exact misattribution bug fixed earlier |
| Batch cost is linear in padded sequence width, and one title can dominate a batch | Every title in a batch pads to the longest, so a single outlier taxes 31 others. Measured **Pearson r = 0.992** between padded width and batch duration (width² fits worse at 0.920, so FFN dominates attention at these lengths). Titles are median 10 tokens, p99 36 — but max 875, and that one batch cost 4.25s against a 0.142s median, **30×**. 34% of embed time was spent above median batch cost. This is the mirror image of the Phase 0 padding finding: equalising width bought no *accuracy*, but width is almost the whole *latency* story |
| Phase 0 baseline, superseded 2026-07-29: **91 clusters / 33.7% noise / 2,671 unique nodes / 5,818 kept** at `--knn 10 --shared 4` | Replaces 104 / 33.7% / 2,699 / 5,873. The corpus changed deliberately: the bare-URL junk rule drops 55 pages and the 64-token cap alters the vectors of the few very long titles. Noise held exactly at 33.7% and the recognisable clusters survived (DSA, job search, the GitHub portfolio), so this is corpus drift from an intended filter change, **not a regression**. Any future baseline comparison must start here |
| §3's context isolation is what keeps the UI alive — validated, not assumed | **87% of the run had a thread blocked, and the dashboard stayed fully responsive**, because every block was in the offscreen document while the dashboard ran in its own context. Had the pipeline lived in the dashboard, or in a worker sharing its thread, this would have been a two-minute UI freeze. The offscreen document is not merely "where the model fits" — it is the isolation boundary that makes a blocking pipeline tolerable at all. Keep heavy work out of the dashboard context permanently |
| The 253ms "model" stage is real, not a mislabel — but the 16.5s embed outlier is still unexplained | Suspected that ORT defers session init to first inference, which would hide the model cost inside the first embed batch. **Measured: it does not.** `pipeline()` takes 174ms and the first inference runs at 0.89× the median of the next five — no deferral, so 253ms in the extension is consistent with Node, not implausible. That leaves the 16.5s stall unaccounted for. `BlockingEvent` now records `sinceStageStartMs`: if the worst stall begins at ~0ms into embed it is lazy setup after all, and anywhere else it is not (GC, throttling, a pathological batch). Caveat: measured with `onnxruntime-node`, so confirm in the extension |
| WASM in the extension is ~3× slower than native ONNX in Node, as expected | Embed median 153.3s in the offscreen document against ~57s for the same corpus size in Node — single-threaded `ort-web` (forced by MV3's CSP, which blocks SharedArrayBuffer and blob workers) versus multi-threaded `onnxruntime-node`. Collapse is only 1.3–2.3× slower, which is the consistency check: it is pure JS on V8 in both, with no WASM involved |
| The model and the ONNX runtime ship inside the package; no `host_permissions` | A runtime download needs a host permission, which puts a second alarming line next to "read your browsing history", and a network call contradicts the pitch. `env.allowRemoteModels = false` makes it structural rather than aspirational: a missing file errors instead of silently fetching. Costs ~42MB packaged (22.6 model + 18.3 ORT wasm). Verified by loading with `fetch`, `http.request` and `https.request` all replaced by throwing stubs |
| `content_security_policy.extension_pages` declares `'wasm-unsafe-eval'` | MV3 blocks `WebAssembly.instantiate` outright without it, so the ONNX runtime cannot start. It permits WASM compiled from bytes already in the package — it is *not* remote code, does not relax `script-src` for JavaScript, and leaves the no-network guarantee intact. Manifest keys carry no comment: JSON has none, and Chrome logs `//`-prefixed keys as errors |
| WASM streaming compilation is unavailable in MV3; the ArrayBuffer fallback is accepted | `WebAssembly.instantiateStreaming` needs `Content-Type: application/wasm`, and nothing controls the response headers for a `chrome-extension://` resource — there is no server config to set. onnxruntime-web 1.14 exposes no `wasmBinary` hook to bypass it either, so the emscripten fallback is the only path. Measured cost: read 8ms + compile 18ms = 26ms, of which streaming would have hidden at most the 8ms read, once per offscreen document. Do not re-investigate |
| Audit of remaining absolute similarity constants | `--min-sim` 0.20 (inert, above); `--threshold` 0.35 (`--algo community` only, a comparison arm); `--dup-threshold` 0.97 (**defensible** — it detects near-*identity*, not topical relatedness: duplicates sit at ~1.0 against a measured cross-topic max of 0.44, so the margin is enormous and scale-stable). §5's 0.40 zero-shot threshold remains unverified *at the time of this row* — now measured and the whole approach disproven, see below. Frequency rules are already scale-relative (`max(5, 0.3%)`) and need no change |
| Junk titles (auth walls, bot checks, error pages) are dropped before embedding | They carry no topic, recur in the hundreds under many distinct URLs so the normalized-URL dedupe never sees them, and once embedded they form dense identical neighbourhoods. Most are auth pages that §9 forbids capturing at all |
| Junk prefix patterns apply only to titles under 40 chars | An interstitial announces itself in a few words. `/^error/` alone eats "Error handling with Result and the question mark operator", a real Rust doc page — length is what separates the two |
| Near-duplicate pages collapse to one weighted node before clustering | LeetCode serves `/problems/two-sum/`, `/description/` and `/submissions/` under one title, so URL dedupe misses them. Twenty copies saturate a page's k nearest neighbours with itself: no related page can link, every problem becomes its own cluster, and unique pages get no mutual edges at all. Measured on real history: 553 clusters, 41% noise, four separate "Sign in" clusters |
| int8 batch-shape drift accepted, not engineered around | A title's vector shifts ~0.993 cosine when its batch has a different padded sequence width. fp32 shows none, so mean pooling masks padding correctly. Padding short batches to 32 rows does not help — width, not row count, is the driver — and top-6 neighbour rank is 100% stable across regroupings, so nothing mutual-kNN reads is affected |
| **Generalises the `coverage` row above: structural quality cannot detect wrong-*topic* prose, and boilerplate is the common case** | The YouTube row framed this as a transcript-vs-description problem. Real captures show it is the whole class, and far more frequent: "Privacy Notice" scored **good 0.90** (a cookie policy), "Search & Apply" **0.95 at 15% coverage** (privacy-policy text), "Work with Concentrix" **0.67** (a recruitment-fraud warning). Legal boilerplate is *perfect* prose — full sentences, high stopword ratio, correct terminators — so it scores at the top of the scale while carrying nothing about what the user was doing. Every structural signal we have measures "is this well-formed text", and boilerplate answers yes. **A high score at low coverage is the tell**: 0.95 at 15% means high confidence in a *fragment* of the page, which is close to the opposite of what a quality score should imply. Open question, not yet decided: should coverage cap the score, so a fragment cannot score above what its share of the page justifies? **The class also includes prose-shaped UI chrome**, not only legal boilerplate: a GitHub dashboard scored **good 0.73** on "Skip to content Dashboard Provide feedback Saved searches Use saved searches to filter your results more quickly…". Help text is written in grammatical sentences, so every structural signal reports prose. This is a **new** failure rather than a survival of the old one — it passes on genuine sentence structure, not on repetition, so the unique-token stopword fix has no purchase on it and could not have caught it. **Phase 3 concern** — these pages will cluster, and a "privacy policy" cluster spanning twenty unrelated employers, or a "dashboard chrome" cluster spanning every SaaS tool, pollutes the taxonomy while looking legitimate |
| Job portals reuse one title across unrelated pages | Two captures both titled "Cognizant Career Portal": one 3,970 chars of a Python/Snowflake job description, one 167 chars of a disability disclosure form. The captured vectors separate them correctly because they are built from body text — but the *backfilled* versions of these same pages carry only the ambiguous shared title, so they are indistinguishable in the index. This is a concrete instance of the heterogeneity problem: the same URL space is well-separated when captured and collapsed-together when backfilled, which is exactly the ingestion-route bias to quantify in the Phase 2 step 2 measurement |
| The dwell gate leaked through a throttled timer — reproduced, fixed | Reported as "captured at 25s active, below the 30s threshold". **The report was two separate things.** The 25s is *correct*: `activeSeconds` and dwell are different numbers, and a page loaded in a background tab has a stale input window, so the first seconds of focused-but-untouched reading legitimately do not count as active. Reproduced exactly — dwell 30,000ms with activeSeconds 25 — and §9 is working as written. **A real leak was found while looking**, though not the reported one: `elapsed` is wall time since the previous tick, and Chrome throttles `setInterval` in a hidden tab to ~once a minute, so the first tick after refocusing credited the whole gap as focused time. Measured: one tick turned ~0.5s of real attention into 60,000ms of dwell and tripped the gate instantly. Fixed by resetting the elapsed clock on every visibility/focus transition and clamping `elapsed` to 1.5 ticks — the same case now yields 500ms and does not capture. A hard assertion at the capture path re-checks the threshold, because this class of leak is silent: the page is simply indexed on attention it never received |
| Adapters named from a single observed failure keep turning out unnecessary — that is now a pattern, not a coincidence | **GitHub** was listed after one profile page returned nav chrome; repo pages then measured good 0.87 at 82% coverage and it was removed. **NeetCode** is now the second candidate: "Reverse Nodes in K-Group" captured at **tier 3, good 0.74, 347 chars**, and the text is the actual problem statement, lifted from the meta description — the ladder handled it without an adapter, which is exactly what §8 says tier 3 is for. Both were named from one page each, before the quality-driven fall-through existed. The lesson is not "stop proposing adapters" but **the ladder must be working before the adapter list means anything**: a rung that silently accepts poor output makes every site look like it needs a bespoke extractor. Still on the §11 list pending 3–4 more NeetCode problems, as one good capture is the same evidence that produced the GitHub mistake |
| Window blur does not leak dwell, but the fix for the throttled-tick leak did | Checked because `visibilitychange` never fires when the whole Chrome window loses focus to another application — the tab stays `visible`. No leak: `isFocused()` also requires `document.hasFocus()`, which goes false in that case, so switching to an editor stops dwell accrual. **A real defect turned up in the fix from the previous session, though.** Its focus/blur listeners used `{ capture: true }`; element `focus`/`blur` do not bubble, but a capture-phase *window* listener still sees them, so every click into a text field reset the elapsed clock and truncated that interval. Measured on a form-heavy page: **22% of dwell lost, and the 30s gate reached 25 seconds late.** Now registered without `capture`, with `visibilitychange` on the document where it actually fires. **The direction is the important part.** It under-counted, so it *suppressed* captures rather than leaking them — and suppression is symptomless: a page that was never captured is indistinguishable from a page that was never visited. Nothing in the corpus, the counts or the dashboard would ever have shown it. The leaking twin of this bug (the throttled tick) was found within one session because it produced a visibly wrong record; this one survived review and shipped. Recorded as a binding rule in CLAUDE.md §14: gates must be tested in both directions |
| A per-page `activeSeconds` filter is the wrong way to exclude passive media, and is the obvious thing to build | Option (c) — capture everything, defer to Phase 3 — was chosen for passive consumption, and the near-miss was recommending `activeSeconds` as the per-page test to go with it. **It does not discriminate.** §9 counts a second as active only if there was input within the previous 60s, so a 40-minute conference talk watched attentively, without touching anything, lands at `activeSeconds` ~0 — identical to an hour of YouTube autoplay. A per-page filter therefore drops precisely the case that made the stricter gating options unacceptable in the first place: real, deliberate, valuable viewing. The discriminator is the **session shape**, not the page: many sequential pages on one domain, all at ~0 active, each short. One long page at ~0 active is attention; twenty short ones in a row are autoplay. Written into §11 Phase 3 as a requirement rather than left as a note, because "Phase 3 filters on activeSeconds" is what gets implemented otherwise — it is the shortest path from the stored field to a filter, and it is wrong |
| Tier 4 moved ahead of the junk drop — the ladder never got a chance at these pages | The bare-URL rule had two justifications: *no topic*, and the 875-token latency outlier. **The 64-token cap fixed the latency independently, so only "no topic" remained — and the audit showed it false for 27 of 30 distinct cases.** §8 has always had a "domain rule only, weak but never empty" rung; it was unreachable for exactly the pages that need it, because the junk filter ran *before* the ladder. Now the path is parsed first and the page is dropped only when derivation yields nothing: `geeksforgeeks.org/batch/skill-up-python/track/...` → "geeksforgeeks batch skill python track article" (6 path words, kept), `cf.legacypoint.site/middle.html?cs=…` → "legacypoint middle" (1, dropped). **53 pages rescued, 2 dropped.** Two flaws found while building it, both from counting the wrong thing: host labels were counted as topic words, which inflated every URL by one or two and wrongly rescued the legacypoint spam URL; and host labels were matched dot-separated while the splitter also splits on hyphens, so `watt-wise-sc5m.onrender.com` contributed three phantom path words. The keep/drop decision now counts **path words only**, with the host still embedded because it is genuinely topical |
| Strip-then-match may confirm a junk judgement, never create one | "Dashboard \| Google Skills" is a real page that survives every rule individually but dies from the combination: the derived-boilerplate stripper leaves "Dashboard", which the exact list then kills. Of the two available fixes — match the exact list against the original as well as the stripped form, or exclude stripped titles from exact matching entirely — the first is correct and the second is not, because "Sign in - Google Accounts" → "Sign in" is a *genuine* hit that the second would lose. So the exact list is matched against both, and a stripped-only match is rejected. 6 pages returned |
| The corporate blocklist matched HR *vendors*, not deployments | `workday` and `servicenow` are SaaS products whose hostnames appear on public careers portals — the opposite of internal tooling. 14 of 18 corporate blocks were job applications during an active job search, in a corpus where job hunting is one of the largest clusters. Both tokens removed; `sso` narrowed from a bare prefix (which caught `sso.yourlearning.ibm.com`, a learning platform) to identity providers as whole hosts. **The honest position is that the discriminator for "internal" is not in the domain name at all** — a vendor-hosted careers page and a vendor-hosted internal portal share a hostname shape. Where no reliable rule exists, prefer under-blocking: a wrongly indexed corporate page is visible and removable via §9's retroactive delete, a wrongly blocked one is invisible forever. 59 pages returned |
| Filters are now inspectable in the dashboard, because this audit only happened by request | §9 already required the blocklist be inspectable; that is extended to every filter. `FilterResult.audit` records each drop with its reason, plus the path-rescued/path-dropped split, and a "What was dropped" panel surfaces blocklist matches by category and domain against the live index. Both bugs the audit found were in the **suppressing** direction and neither left any trace in the product — a filter the user can see is self-correcting, one they cannot is permanently suspect |
| Phase 0 baseline, superseded 2026-07-30: **103 clusters / 33.4% noise / 2,714 unique nodes / 5,871 kept** at `--knn 10 --shared 4` | Replaces 91 / 33.7% / 2,671 / 5,818. +53 pages returned by tier-4 path extraction and +6 by the strip-then-match fix (the corporate blocklist fix returns a further 59, visible only in the extension since validate runs with the blocklist off). Cluster count rose 91 → 103 and noise fell slightly; the returned pages are job listings, course pages and profile URLs that form small real clusters rather than joining existing ones. Corpus drift from three intended filter corrections, **not a regression** |
| The "+59 pages from the corporate fix" figure was wrong, and the audit was right | Reconciled: the corporate fix returned **18 pages**, exactly matching the 18 rows the audit reported. The 59 was the *whole blocklist's* page-level impact (212 raw rows collapsing to 59 unique pages), which I attributed to one of its rules. Nothing was under-reported |
| **The pipeline counts in two units, and mixing them made a correct audit look broken** | Pass 1 runs before deduplication and counts **raw history rows**; pass 2 runs after and counts **unique pages**. `droppedBlocked` is 212 rows; the same blocking changes `kept` by 59 pages. Both correct, not comparable. `DROP_UNIT` now records the unit for every drop reason and the dashboard prints it per row, because a suppression panel that quietly mixes units is worse than none — a user reading 18 where 59 pages moved concludes the filter is fine and stops looking. **Under-reporting suppression is as symptomless as the suppression itself** |
| The first "What was dropped" panel reported near-zero and would have given false reassurance | It scanned *stored* pages and asked what the blocklist would match — but blocked pages are never stored, so the answer was always ≈0. A panel built to make suppression visible would have testified that nothing was suppressed. It now reads the audit **recorded during the run** (`BackfillSummary.filterAudit`, ~11KB) rather than re-deriving anything from the surviving index |
| Drop counts must reconcile to raw − kept, and the failure must surface | `summariseAudit` computes `unaccounted = raw − kept − Σ(drops)` and sets `reconciles`. A non-zero value means some filter is removing pages that no reason accounts for — the exact shape of an invisible suppression bug. It is logged as an error by the backfill and rendered as a red banner in the panel rather than being swallowed. Currently reconciles exactly: 12,302 raw − 5,812 kept = 6,490 = Σ drops |
| Superseded — the heterogeneity mechanism assumed titles vs up-to-8000-char text; the actual gap (if any) is titles vs the first ~64 tokens of text | `embeddings.ts` sets `tokenizer.model_max_length = EMBEDDING_MAX_TOKENS` (64) once on the shared tokenizer, so it truncates every `embed()` call, not only titles — a captured page embeds roughly the first 250 characters of its up-to-8000-char stored text, the same window a title gets. The short-title-vs-long-text gap that motivated `vectorSource` may be far smaller than assumed, or may not exist, once both routes truncate to the same length. Not yet measured either way — see the three-condition design below, which replaces the two-condition one this row's CLAUDE.md §14 row previously described |
| The 64-token cap was a decision about titles; body text inherited it without one | Chosen to fix the 875-token title latency outlier (§14 "Titles are truncated to 64 tokens…"), where it left 99.8% of titles untouched — the design target was the title length distribution, not the ~250-token median of captured body text. It sits on the tokenizer object, which every `embed()` call shares, so it applies to text calls by construction, not by anyone deciding text should be capped there too. Whether text needs its own, larger cap is exactly what the three-condition measurement is for |
| The unimproved compositional-query result (§15) may share this cause — not confirmed | §15 expects compositional queries to improve once Phase 2 replaces titles with page content. If captured text is truncated to the same 64-token window as a title before embedding, that replacement has not actually happened in the way §15 assumed — most of the extracted text is discarded pre-embedding, not fed to the model. Revisit §15 after the three-condition measurement lands; do not edit it on this suspicion alone |
| Measurement design: three conditions, not two, and per-condition threshold re-checks | (a) title at 64 tokens, (b) text at 64 tokens (current), (c) text at 256/512 tokens, on the same pages, with per-query embedding latency reported for each (the padding-width finding applies again: cost is linear in sequence length). The 0.97 collapse threshold and the 0.194/0.021 within-/cross-topic medians are re-measured under each condition rather than once, because (b) and (c) are different vector spaces for the same page — a pair that looks like a near-duplicate at 64 tokens (identical opening) can diverge at 256, and a threshold safe for one condition is not thereby safe for another |
| **Decision: `EMBEDDING_MAX_TOKENS` stays 64 for both titles and text** | Measured on 107 pages, 15 realistic queries, three runs (similarity and cluster numbers were bit-identical across runs — this corpus size is fully deterministic under the model; only latency varied, as the standing rule predicts). **(1) Peak query similarity falls monotonically as the cap rises**, 0.942 → 0.788 (title → text@512), while the bulk of the distribution barely moves (pooled mean 0.164 → 0.168): the only real effect of more content is a worse best match, not a better one. **(2) Cost is prohibitive:** text@512 extrapolates to ~12 minutes to embed the ~5,200-page corpus already measured for blocking (§14) — 12× the text@64 cost, for worse peak matching. **(3) The pairwise-similarity floor (p50) rises** 0.161 → 0.313 with cap: more embedded text means more shared boilerplate across unrelated pages, so the whole corpus looks more alike — semantic-space boilerplate pollution, the same class already logged for structural quality scoring, now showing up in the vectors themselves. **(4) Caveat:** several queries paraphrased real page titles in this job-search-heavy corpus, which structurally favours the title condition — the latency and peak-dilution findings hold independently of query design, but the *size* of title's measured advantage does not necessarily generalise. **(5) One genuine text win:** "leetcode reverse linked list in groups of k" improved 0.656 → 0.782 → 0.787 → 0.788 — almost the entire gain lands between 64 and 256 tokens, then flattens, so **256, not 512**, is the number worth revisiting if this is ever reopened, and only for technical/compositional queries a title cannot carry. Collapse (0.97) verified safe in all four conditions, 0.35–0.45 margin. Within-/cross-topic medians shift up to 67% between text@64 and text@512 on the same corpus and remain uncalibrated for production — the 107-page sample is too small and topically narrow to set a production threshold from directly |
| Superseded — the compositional-query hypothesis is **disproven**, not merely unconfirmed | The prior row here said "not confirmed; revisit after the three-condition measurement lands." It has landed: more page content does not fix term-independent matching, and makes at least one case measurably worse — "telugu job search youtube video" correctly top-1's a job-search video at title@64 and text@64, then switches to an unrelated personal-finance vodcast at text@256 and text@512. CLAUDE.md §15 no longer frames this as something Phase 2 or a larger cap will fix |
| **All three tier-1 adapters eliminated by measurement — §8's budget is now zero, and that is a stronger result than building them** | **GitHub:** proposed after one *profile* page returned nav chrome; repo pages then measured good 0.87 at 82% coverage, and the ladder already demotes the profile and sub-tab pages that fail. **NeetCode:** proposed as a JS-rendered problem statement invisible to Readability; tier 3's meta description turned out to carry the actual problem statement, good 0.74 at 347 chars. **YouTube:** built, tested across three rounds, removed after three *distinct* DOM failure modes — a stale segment container selector (`segmentNodes=0` after a full 6s with the button found and clicked), an overflow-menu layout variant (`button-not-found` at 202 buttons scanned, `more-actions-button-present=true`), and two successes that were both auto-captioned Telugu videos. Fixing it meant opening the overflow menu with synthetic clicks on a page the user is actively watching **and** chasing a segment selector across YouTube's A/B variants indefinitely — permanent maintenance against someone else's UI redesign cycle, for one site. **The general finding is the point: the quality-driven ladder made per-site adapters unnecessary.** Every adapter was proposed from a single observed failure, and every one of them was proposed *before* the ladder could fall through on a quality verdict — a rung that silently accepted `poor` output made each site look like it needed a bespoke extractor. §8's "write 5–8 adapters maximum" is superseded by a measured need of **zero**. Tier 1 stays in the type and the table as an extension point; the bar for using it is now frequent, measurably failing *after* fall-through, and fixable without an ongoing maintenance commitment |
| The YouTube adapter paid for itself anyway, by failing | Debugging why it never fired is what exposed the empty-`catch` capture failure below — which was silently dropping **every** capture in any tab left open across an extension reload, corpus-wide and not YouTube-specific. Three diagnostic rounds bought a permanent fix to the delivery path, a `console.debug`/Verbose lesson, the playback-param navigation bug, four new preflight assertions and a capture-health panel. Worth considerably more than a transcript. Recorded because the instinct on a dead-end feature is to count it as wasted time; the adapter was the *instrument* that made a much larger silent failure visible |
| **An empty `catch` on the delivery call is the purest form of the suppression class** | `capture()` ended in `try { await chrome.runtime.sendMessage(...) } catch {}` — the one call that decides whether a capture exists at all, with every failure discarded: no log, no counter, no UI trace, nothing in the corpus. The justifying comment claimed *"the page is not lost — a return visit re-captures it"*, which is true for a worker mid-restart and **false for the dominant failure**. Reloading or updating an extension **permanently orphans the content script in every already-open tab**: it keeps running, keeps accruing dwell, keeps extracting, and every `chrome.*` call throws `Extension context invalidated` for the life of that tab. A tab reload is the only recovery, and nothing prompted one. Measured: capture stopped at 4:01 PM and every page browsed afterwards was silently dropped across two dev rebuilds — 141 stored pages, zero queued, no error anywhere, and the symptom was indistinguishable from not having browsed. **This is a standing hazard of the development loop itself**, not a one-off: every rebuild during a session silently kills capture in any long-lived tab. Now detected by name, logged at `console.error`, and the dwell timer is stopped, since an orphaned tab can never deliver and ticking on only accrues attention nobody can receive |
| Diagnostics on the capture path use `console.log`, never `console.debug` | Chrome renders `console.debug` at **Verbose**, which is hidden unless that box is ticked in the DevTools level dropdown. A full diagnostic round was spent concluding "the content script is not running" from an empty console, when the code may well have been running and logging the whole time — every line it emitted was below the default filter. The cost is not the hidden log; it is that an invisible log reads as *proof of absence*. Unrecoverable failures go to `console.error` so they survive any filter setting |
| Navigation detection must ignore playback-position params | `navigationKey()` compared `pathname + search`, and YouTube rewrites `?t=` on every seek and `pp` as it cycles player flags. Each rewrite read as a navigation, reset the dwell clock, and meant **the 30s gate was never reached on a long video** — silently, and worst on exactly the long-form content most worth capturing. Stripped generally rather than per-site (`t`, `start`, `end`, `time_continue`, `pp`, `ab_channel`, `feature`), since Vimeo and Twitch spell it the same way. `v` is deliberately **not** stripped: it is the video's identity, and removing it would make every YouTube video look like one page — a far worse bug than the one being fixed. Verified in both directions per the gate rule: seeks, `t` churn and `pp` churn do not reset; changing video, changing playlist, changing search query and leaving `/watch` all do |
| Preflight now asserts the content script still *behaves*, not merely that it exists | The old checks — file present, no ESM syntax — would pass an empty, truncated or half-tree-shaken `content.js` cleanly, and a content script that loads but does nothing is the worst failure available here, because capture stops with no error and a page never captured looks exactly like a page never visited. Now checks four behaviours that must survive bundling (`setInterval`, the `pagehide` listener, the `CAPTURE_PAGE` message, `visibilitychange`), keyed on string literals and API names rather than identifiers, which minification renames. Also a size floor and a **staleness guard**: `npm run build` runs two vite passes and the content pass is second, so a failure there leaves the *previous* `content.js` beside fresh output from the first pass — dist looks built while the extension runs yesterday's capture logic. The guard earned itself immediately, catching a genuinely stale build on its first run |
| Phase 2 step 3 — §9's remaining controls, verified both directions | Pause (popup: 30min/1hr/indefinite), retroactive removal (dashboard: time range or individual pages), export (JSON/Markdown), delete range (settings, separate control). Pause is checked in the content script *before* extraction and re-checked in the background at queue time — the same defense-in-depth already used for the blocklist, since the content script runs in a page the extension does not control. A new `default_popup` in the manifest means `chrome.action.onClicked` never fires again; the dead `onActionClicked` listener was removed rather than left in place, and the popup calls `openDashboard()` directly instead, since popup pages have the same `chrome.tabs`/`chrome.windows` access as the background |
| Deletion needed a queue-cleanup step that was not obvious from the request | A page can be captured, queued and not yet embedded at the moment it is deleted. Deleting only from the pages store would let the next drain write it straight back a minute later — "delete" undoing itself silently. `deletePages` now also calls the existing `removeFromQueue`, matched by the same normalized-URL-hash id both stores already key on |
| The search index needed an explicit invalidation path that did not exist | `searchIndex.ts`'s index is offscreen-document-local module state; a delete in the dashboard is invisible to it. Without telling it, a "removed" page would keep surfacing in search until the next backfill happened to rebuild the index — a correctness gap the retroactive-removal feature would have shipped with silently. Added `INVALIDATE_SEARCH`, routed dashboard → background → offscreen the same way `SEARCH` already is, and every delete path calls it |
| GroupRecord membership is left stale on delete, deliberately | A deleted page's id can still appear in a `GroupRecord.memberIds` array after deletion. Checked whether this matters: `searchIndex.ts` builds its representative list by filtering `pages.filter(p => collapsedBy.has(p.id))` — since `pages` no longer contains the deleted id, it silently drops out of the index with no crash and no `getPage` null to handle. Worst case is a stale `size` slightly overcounting a `collapsed` badge until the next backfill recomputes groups. Not worth cleaning up given §5's "full recompute weekly, no incremental clustering" — a fix here would be scaffolding for a state groups are never treated as durable across anyway |
| Both-directions verification, run against `fake-indexeddb` (installed with `--no-save`, removed after) | **Delete-range**: 5 synthetic pages, 2 inside `[start, end]` and 3 outside (including a boundary case at exactly `start`). `deletePagesInRange` removed precisely the 2 in-range and left all 3 out-of-range pages — including the boundary page, confirming `IDBKeyRange.bound` is inclusive as assumed. **Queue cleanup**: two pages each with a matching queue entry; deleting one removed its queue entry and left the other page *and* its queue entry untouched — not over-clearing. **Pause**: an active timed pause reports `isPaused` true with ~30min remaining; an *expired* one reports false rather than staying stuck blocked; `indefinite` stays true arbitrarily far in the future; `notPaused()` is false. **Pause cannot delete anything** by construction, not by test — confirmed by reading rather than running: `lib/pause.ts` has zero imports and `platform/pause.ts` imports only `lib/pause.js` plus `chrome.storage.local` on a single key; neither file can reach `lib/storage.ts`'s deletion functions, so there is no code path for a pause action to touch page storage even in principle |
| **Structural quality ranked junk above real content — the first time junk outscored genuine content rather than merely passing on its own** | On a capture of "Naga Chaitanya SCUZI," player chrome plus the logged-in username picked up from the comment box scored **good 0.73**; the transcript adapter's own real transcript for the same class of video scored only **0.67, flagged non-Latin** (stopword component excluded, per the component-count rule above). Fourth distinct case of prose-shaped junk passing structural quality — after the YouTube description (coverage row), legal/recruitment boilerplate, and GitHub dashboard chrome, all above — and the first where junk beat real content on score rather than only passing in isolation. The ladder's "accept the moment verdict is not poor" path protects this specific instance by *order*, not by *score*: tier 1 is checked before Readability, so a non-poor transcript is returned before Readability's chrome ever runs. But the ladder's other path — "everything poor, take the highest score" — is a pure score comparison with no notion of topical correctness, and this measurement shows junk and real content can land on either side of that comparison, junk ahead. Not fixed; recorded as a live instance of the risk already named in the abstract above |

| **Zero-shot classification measured and rejected — the approach, not the threshold** | §5 specified ~0.40 against ~25 pre-embedded seed labels; §14 flagged it unverified. Measured over the real corpus (5,374 pages: 5,225 title-derived, 149 text-derived), three runs, similarity numbers bit-identical across all three. **At 0.40 only 13.3% of title pages and 10.1% of text pages classify** — 87% falls into the "discovery queue", which makes the queue the corpus. Distributions: best-label score median **0.250** (title) / 0.271 (text), p90 0.426, max 0.723; margin between the top two of twenty-five labels has median **0.036** and p10 **0.005**. Title and text scales are close, consistent with the resolved heterogeneity row — both truncate to 64 tokens, so ingestion route is not the axis that matters here. Measurement used the *stored* vectors exported from IndexedDB rather than re-embedded titles, because the shipped vector came from `embedText` (boilerplate-stripped, or path-derived at tier 4) and re-embedding `title` would have measured a corpus that does not exist |
| Lowering the threshold does not help, and that is a distinct finding from the 13.3% | On 20 pages with an unambiguous correct label, **correct-label scores span 0.091 to 0.473** — no cutoff separates right from wrong anywhere in that range. A 0.25 threshold would reject the correct label on the biryani recipe (**0.091**), the film trailer (**0.097**), and *"Longest Substring Without Repeating Characters"* (**0.140**) — the last of which the classifier had already ranked **#1 correctly**, so the threshold would discard a right answer while admitting confident wrong ones. Top-1 accuracy was 12/20 |
| **Rank-and-margin does NOT rescue this, and it is the fix that will be proposed again** | Rank-and-margin is the standing solution to absolute-threshold problems in this project — it replaced the fixed cosine cutoff in clustering and is recorded in §14 as the general lesson. It does not transfer here. `Application History \| Mynaukri` — a job-application tracking page — scores **0.467 on `history` with a 0.277 margin**: above every threshold considered *and* in the top decile of margins, and the match is pure lexical overlap with the word "History". `Applied History` scores 0.723 with a 0.338 margin. `Home - Presenti` lands on `home improvement`. **Margin measures separation, not correctness**, and these false positives are well separated. The distinction worth keeping: clustering's failure mode was *scale drift* (right neighbours, wrong absolute numbers), which rank fixes; this failure mode is *confident wrongness* (high score, high margin, wrong answer), which rank cannot see. Same-looking symptom, different problem. Combined gates only trade coverage away without addressing it — `score≥0.25 & margin≥0.05` classifies 32.0%, `margin≥0.10` classifies 16.7%, and the `history` false positives survive both |
| The structural reason: a 64-token title has no path to the inference | ~1,100 pages of this corpus are LeetCode/NeetCode problems, the single largest real category, and they are the worst served. **`3Sum` → business (0.223), `Two Sum` → finance (0.173)**, with `programming` ranked #5 and #7 respectively. Those titles are about arithmetic and objects; nothing in them lexically resembles the word "programming", and the model is not being asked a question it can answer from ten tokens of problem name. This is not a tuning failure — it is the input length and the model class, the same limit already recorded for compositional queries |
| Expanded label text measured and rejected | Replacing bare labels with short phrases (`"programming, software development, writing code"`) raised judged accuracy **12/20 → 14/20** and fixed shopping, health and both entertainment cases. Rejected anyway: the distribution **did not lift** (median 0.250 → 0.242), programming got **worse** (`3Sum` → entertainment, `Two Sum` → history), and per-label authoring smuggles domain knowledge into the one hardcoded list §6 requires to stay universal. A fix that improves a 20-page judged set while leaving 5,374 pages unchanged is a fix to the judged set |
| Per-label z-scoring and label-relative rank — considered, rejected without measurement | Both address the *attractor-label* problem, which is real: `career` and `programming` absorb 40% of the corpus while `cooking` wins almost nothing, and normalising each label against its own distribution (or assigning a page only if it is in that label's top-N) would rebalance that. Neither was measured because neither touches correctness: **`Applied History` → `history` survives both** — it is that label's strongest match by a wide margin, so it ranks first under label-relative rank and z-scores high under per-label calibration. Rebalancing which labels win does not make the winners right. Recorded so this is not re-proposed as untried |
| Resulting design: seeds are a weak prior, clustering is the classifier | §5, §6 and §11 Phase 3 rewritten together. Seed labels ship in `src/lib/topics.ts` and do exactly one job — keep day one non-empty. **They are never displayed as a topic and never written into `PageRecord.topics`**; only a derived cluster earns that. Phase 3 step 1 is therefore complete with a negative result whose deliverable is a design decision rather than a classifier, and clustering is promoted from "supplement to zero-shot" to the primary classification mechanism. Nothing downstream may assume a page arrives with a usable label — the unclassified pool is the normal state until a cluster forms, which raises the stakes on the boilerplate-pollution warning already in Phase 3 |
